# ── 데이터 & 학습 산출물 (용량 큼 · 재생성 가능) ───────────────
# raw JSON / 전처리 parquet / 학습된 모델(.txt)은 커밋하지 않는다.
# 재현: requirements 설치 후 `python train_races.py ...` 로 다시 생성.
# 포트폴리오용 핵심 산출물(계약·지표·시각화)은 results/ 에 큐레이션해 커밋한다.
data/

# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ── 가상환경 ─────────────────────────────────────────────
.venv/
venv/
env/

# ── 에디터 / OS ──────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 잡파일 ───────────────────────────────────────────────
/0
*.log
